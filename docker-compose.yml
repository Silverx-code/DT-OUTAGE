services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dt_outage
      POSTGRES_USER: dt_outage_app
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - dt_outage_pgdata:/var/lib/postgresql/data

volumes:
  dt_outage_pgdata:
